
import java.util.*;

public class NonRep {

    public int non(char[] x) {
        int left = 0;
        int window = x[0];
        HashSet<Character> map = new HashSet<>();
        for (int right = 0; right < x.length; right++) {
            char c = x[right];

        }
    }
}
